const GridBackground = () => {
  return (
    <div className="background-grid absolute inset-0 -z-10 flex w-full items-center justify-center">
      {/* Radial gradient for the container to give a faded look */}
    </div>
  );
};

export default GridBackground;
